- title "Tasquee::" + t(:settings), false
%h2.ribbon.flag= t(:settings)

#users_list
  %h3= t(:calendar_order)
  %ul{:id => "users"}
    - @users.each do |user|
      %li{:id => "user_#{user.id}"}
        %span.handle
          \::
        = user.name
